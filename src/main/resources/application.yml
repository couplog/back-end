spring:
  profiles:
    active:
      - local
    group:
      local:
        - db-local
        - logging-local
        - web
      dev:
        - db-dev
        - logging-dev
        - web
    include:
      - db
      - logging
      - web
